owners:
    - 163130634
applications:
    - name: brawlgg-server
      github:
        owner: Team-selfton
        repo: BrawlGG
        branch: main
        installationId: "98579662"
      build:
        nodejs:
            nodeVersion: "20"
            buildCommand: npm install
            startCommand: sh -c 'export BRAWL_API_TOKEN="${BRAWL_API_TOKEN_P1}${BRAWL_API_TOKEN_P2}"; npm start'
      endpoints:
        - port: 3000
