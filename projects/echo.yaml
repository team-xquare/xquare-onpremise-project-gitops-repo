applications:
  - name: echo-be
    build:
      gradle:
        buildCommand: ./gradlew bootJar -x test
        jarOutputPath: /build/libs/*.jar
        javaVersion: "17"
    endpoints:
      - port: 8080
        routes:
          - echo-be.dsmhs.kr
    github:
      branch: prod
      installationId: "105177193"
      owner: Echo-dsm
      repo: Echo-BE
      triggerPaths:
        - /**
addons:
  - name: echo
    storage: 4Gi
    type: mysql
