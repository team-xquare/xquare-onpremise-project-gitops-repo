applications:
  - name: silent-flowers-prod
    tier: x3.micro
    github:
      owner: Silent-flowers-bloom-gracefully
      repo: backend
      branch: master
      installationId: "68917128"
      hash: latest
    build:
      gradle:
        jdkVersion: 21
        buildDir: /
        outputDir: /build/libs/Silent-Flowers-0.0.1-SNAPSHOT.jar
        buildCommand: ./gradlew build -x test
    endpoints:
      - port: 8080
        routes:
          - silent-flowers.xquare.app
